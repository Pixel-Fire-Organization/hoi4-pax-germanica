# Pinned so `build_assets.py --check` is reproducible between local and CI.
# DDS is encoded by our own writer (version-independent); TGA is Pillow's uncompressed
# output, so pinning Pillow keeps those bytes identical. PNG is zlib-compressed and its
# bytes are NOT portable across platforms, so --check compares PNG by decoded pixels.
pillow==12.2.0
